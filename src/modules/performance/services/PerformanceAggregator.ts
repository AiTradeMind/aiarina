export class PerformanceAggregator {
  public aggregate(metricsList: any[]) {
     // Aggregates multiple metrics into a single summary view (e.g. Org level view from Portfolios)
  }
}

export const performanceAggregator = new PerformanceAggregator();
