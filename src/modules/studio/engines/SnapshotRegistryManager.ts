export class SnapshotRegistryManager {
  async registerSnapshot(snapshotType: string, snapshotData: any): Promise<void> {
    // Repository call to register snapshot
  }
}
export const snapshotRegistryManager = new SnapshotRegistryManager();
